sysdbusconfdir=$(sysconfdir)/dbus-1/system.d/
initdir=$(sysconfdir)/init.d

init_SCRIPTS = \
    glucose-board-daemon

sysdbusconf_DATA = \
    org.freedesktop.GlucoseBoard.conf

EXTRA_DIST = \
    glucose-board-daemon.in \
    $(sysdbusconf_DATA)
